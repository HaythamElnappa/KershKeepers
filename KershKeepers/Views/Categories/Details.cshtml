@model KershKeepers.Models.EFModels.Category

<div class="container">
    <h2>Details</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <td>
                <img src="@Url.Content(Model.Image)" style="width:100px;height:100px" class="rounded-circle" alt="Circular Image">
            </td>
        </tr>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <td>
                @Html.DisplayFor(model => model.Name)
            </td>
        </tr>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsDeleted)
            </th>
            <td>
                @Html.DisplayFor(model => model.IsDeleted)
            </td>
        </tr>

        <tr>
            <th></th>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = Model.CategoryId })*@
                @Ajax.ActionLink("Edit", "Edit", new { id = Model.CategoryId }, new AjaxOptions { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "Category" }, htmlAttributes: new { @class = "btn btn-primary" }) |
                @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
            </td>
        </tr>
    </table>
    </div>


@*<div class="modal" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                @Html.DisplayFor(model => model.Name)
            </div>
            <div class="modal-body">

                <div class="col-md-4">
                   <img src="" alt="teste" class="img-thumbnail"     style="width:100px;height:100px">
                    <img src="@Url.Content(Model.Image)"  class="img-thumbnail" alt="Circular Image">
                </div>
            </div>

            <div class="modal-footer">

                <div class="text-right pull-right col-md-3">
                    @Ajax.ActionLink("Edit", "Edit", new { id = Model.CategoryId }, new AjaxOptions { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "Category" }, htmlAttributes: new { @class = "btn btn-primary" }) |

                </div>

                <div class="text-right pull-right col-md-3">
                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })

                </div>

            </div>
        </div>
    </div>
</div>*@